// Remove String Spaces

// Simple, remove the spaces from the string, then return the resultant string.
// use replace() with a space globally to replace with no space 
function noSpace(x){
    return x.replace(/ /g, '')
  }